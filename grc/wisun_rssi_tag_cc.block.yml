id: wisun_rssi_tag_cc
label: rssi_tag_cc
category: '[Wi-SUN]'

templates:
  imports: from gnuradio import wisun
  make: wisun.rssi_tag_cc(${n_samples})

parameters:
- id: n_samples
  label: n_samples
  dtype: int

inputs:
- domain: stream
  dtype: complex

outputs:
- domain: stream
  dtype: complex

file_format: 1
