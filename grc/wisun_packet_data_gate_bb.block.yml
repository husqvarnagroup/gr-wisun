# SPDX-FileCopyrightText: 2026 GARDENA GmbH
#
# SPDX-License-Identifier: GPL-3.0-or-later

id: wisun_packet_data_gate_bb
label: packet_data_gate_bb
category: '[Wi-SUN]'

templates:
  imports: |-
    from gnuradio import wisun
    import pmt
  make: wisun.packet_data_gate_bb(${length_tag})

parameters:
- id: length_tag
  label: length_tag
  dtype: string
  default: wisun-packet

inputs:
- domain: stream
  dtype: byte

outputs:
- domain: stream
  dtype: byte

file_format: 1
